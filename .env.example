# Homepage 独立 Compose 配置。复制为 .env 后填写。

# 镜像来源（GitHub Actions 构建并推送到 GHCR）。
# 改成你的 GitHub 用户名（小写）；compose 用它拼出 ghcr.io/<owner>/homepage:latest。
HOMEPAGE_IMAGE_OWNER=your-github-owner

# 图标高清解析：留空=禁用（域名应用只能取 /favicon.ico，低清）；
# 配成 .nas.lan 后，后端对 *.nas.lan 目标转发 authelia_session，
# 解析应用首页 <link> 取原始高清/矢量图标（LAN/IP 目标永不带 cookie）。
HOMEPAGE_COOKIE_DOMAIN=.nas.lan

# SSRF 防护：允许抓取的内网 CIDR（逗号分隔）。留空=所有私网 IP 被拒，
# LAN 模式应用图标取不到（回退色块）。配成你的网段即可放行 LAN 应用图标。
HOMEPAGE_LAN_CIDRS=192.168.0.0/16,10.0.0.0/8
